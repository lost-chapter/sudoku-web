# 作業の引き継ぎ(別の端末・別の担当者)

**この文書は「別の端末で作業を再開するとき、何を持っていく必要があるか」だけを扱う。**
何をすべきかは [実装の進め方と現在地](implementation-roadmap.md) が入口である。

引き継ぎで落ちるのは、**Git に入っていないもの**と**片方の端末にしか無いもの**である。
順に潰す。

---

## 0. 先に確かめる —— 未プッシュのコミットが無いか

**これがいちばん多い落とし穴である。** このプロジェクトは
[開発上の約束](../../AGENTS.md)で**指示があるまでプッシュしない**ことにしているため、
**作業がローカルにしか無い状態が常態**になる。

**ローカルのブランチを全部見る。** 作業ブランチが 2 本以上あるので、
いま居るブランチだけ確認すると取りこぼす。

```bash
# ローカルの全ブランチについて、origin/develop より進んでいる数を数える
git for-each-ref --format='%(refname:short)' refs/heads | while read b; do
  printf '%-24s %s\n' "$b" "$(git rev-list --count origin/develop.."$b")"
done
git status --short
git stash list
```

**0 でないブランチがあれば、その端末を離れる前に必ず共有する。** 手順は 2 つ。

1. `ops-branch-merge` スキルで各作業ブランチを `develop` へマージする
2. **ユーザーの指示を得てから** `git push origin develop` する

⚠️ **SSH の鍵はこのセッションから使えない**(2026-08-05 実測。`ssh-add -l` が空)。
`gh` が認証済みなので、**設定を変えずに一時的な認証ヘルパで送る**。

```bash
git -c credential.helper='!gh auth git-credential' \
  push https://github.com/Tenryu/sudoku-web.git develop:develop
```

**2026-08-05 に初回のプッシュを実施済み**(129 コミット)。CI 4 ジョブは実機で通った。

⚠️ **その後の分は手元に留めてある**(2026-08-05・ユーザーの判断)。
**`develop` はマージ済みだがプッシュしていない。**
工程 4 の完了(E2E とコントラスト)と **5 つ目の CI ジョブは実機で回していない。**
**次にプッシュする人は、E2E ジョブの初回が 1 分ほど長いこと**
(ブラウザ 196 MB の取得。2 回目以降はキャッシュが効く)**を見込んでおくこと。**

`git stash` の中身と、コミットしていない作業ディレクトリの変更も同様に消える。
`git status` が clean であることまで確認する。

### コミットされていない知識も落ちる

**セッション間でやり取りした判断は Git に残らない。** 複数のセッションで進めるときは
メッセージで調整することになるが、そこで決まったことは**文書へ書かない限り消える**。

このプロジェクトはそれを前提に、**判断は必ずどれかの文書へ落とす**運用にしている。

| 種類 | 置き場所 |
|------|---------|
| 人が決める必要があること | [保留中の判断事項](../reference/pending-decisions.md) |
| 設計の判断とその理由 | [decisions/](../decisions/)(ADR) |
| 着手前の調査 | `docs/reports/YYYY-MM-DD-*.md` |
| 実装の理由・落とし穴 | `docs/algorithms/` `docs/ui/` と**コードの doc コメント** |

**「会話で説明したから大丈夫」は引き継ぎでは成立しない。**

---

## 1. 環境を揃える

**`ops-dev-environment-setup` スキルを使う。** ここでは繰り返さない。

Git 管理外で、クローン直後や新しい worktree で**必ず不足する**ものは次のとおり。

| 対象 | 単位 |
|------|------|
| `node_modules/` | worktree ごと(`pnpm install`) |
| `.claude/settings.local.json`(Claude Code のローカル設定) | worktree ごと |
| `packages/web/dist/` | ビルドのたび |
| `puzzles/generated/` | 生成のたび(マニフェストのシードから作り直せる) |
| `docs-html/` | `pnpm docs:html` のたび |

**Git 管理外が増えたらこの表へ足す。**

---

## 2. 現在地を実測で確かめる

**ドキュメントの記載を信用せず、まず動かす。**
[ロードマップ](implementation-roadmap.md)の数値は更新漏れがありうる。

```bash
git log --oneline -5 | cat
git status --short

pnpm install
pnpm test
pnpm typecheck
pnpm lint
pnpm build
```

2026-08-05 時点の実測値。**ここから乖離していたら、その差が引き継ぎで落ちたものである。**

| 項目 | 値 |
|------|-----|
| テスト | **282 件**(core 126 / generator 16 / docs-html 19 / web 121) |
| 型チェック | 4 パッケージ 0 エラー |
| Lint | 0 エラー |
| 本番ビルド | 成功(JS 345.11 KB / gzip 106.65 KB、**CSS 60.88 KB / gzip 10.34 KB**) |

**工程 2 の 1〜7(agent-b)と工程 3・工程 4(agent-c)が入った時点の値。**
統合のたびに測り直す(各自の手元の値を足しても合わない)。

詳細と前提(Node 22 以上・pnpm 11 系)は [ローカル環境の構築](local-setup.md)。

---

## 2.5 まず一枚絵を見る(2026-08-06 に追加)

**[進捗の一枚絵](../assets/progress.html) をブラウザで開く。**
**工程・いま止まっているもの・体制・数値が 1 画面に入っている。**

⚠️ **一枚絵は要約である**。判断の根拠はこの文書と
[ロードマップ](implementation-roadmap.md) にあり、**そちらが正本。**
**管理役が統合と差し戻しのたびに更新する**
([並列エージェントの運用](parallel-agent-operations.md#-管理役は進捗の一枚絵を保つ2026-08-06-に追加))。

## 3. いまの体制

**管理役 1 + 実装 3 の 4 体制**(2026-08-06 に agent-d を追加)。

**agent-a は管理役で、実装は持たない**(統合・ロードマップ更新・判断の集約を行う)。

| 呼び名 | ブランチ | 作業場所 | 担当 | 状態 |
|--------|---------|---------|------|------|
| agent-a | `feature/agent-a` | メインの作業ディレクトリ | **管理役**。統合・文書更新・判断の集約。**例外として工程 1(開発基盤)と CI は agent-a が通す** | ✅ 工程 1 と CI 完了。統合と裁定を継続 |
| agent-b | `cc/agent-b` | `.claude/worktrees/agent-b-7e214c` | **`core` + `generator`**。盤面表現 → 探索ソルバ → 生成 → 穴あけ → 手筋ソルバ → 難易度評価 → パック出力(工程 2) | ✅ **工程 2 完了**。工程 6 の削除も完了。**待機中** |
| agent-c | `cc/agent-c` | `.claude/worktrees/agent-c-baa07a` | **`web`**。盤面の描画 → セル入力 → 状態管理 → 問題の取得 → 進行の保存(工程 3〜4)。**工程 6 のスマートフォン対応とあきらめるボタン** | 🚧 **工程 6 を実装中** |
| **agent-d** | `cc/agent-d` | `.claude/worktrees/agent-d-d4ac0f` | **ドキュメントと `tools/docs-html/`**。mermaid での図解と HTML の読みやすさ(2026-08-06 追加) | ✅ **完了・統合済み(2026-08-06)**。和文の強調・表の見出し固定・⚠️ の箱・コードの色・**図 4 枚**・索引の総ざらい |

### ⚠️ agent-d を足したら `pnpm lint` が壊れた(2026-08-06)

**worktree はリポジトリの複製なので、そこにも `tsconfig.json` がある。**
typescript-eslint が**「tsconfigRootDir の候補が複数ある」と言って 1 つも lint できなくなった。**

**`eslint.config.js` の `ignores` に `.claude/**` を足して直した。**

⚠️ **CI には worktree が無いので、手元でだけ落ちる種類の壊れ方である。**
**次に人を増やすときも同じことが起きる**(除外済みなのでもう起きないが、
「増やした瞬間に壊れる仕掛けがある」ことは覚えておく)。

**ブランチの規約と統合の権限は [ブランチ戦略](branch-strategy.md) にある。**
worktree のディレクトリ名の末尾は Claude Code が付けたもので、意味は無い。

### この分け方の理由

**工程 1 は分割しない。** 全員の前提になる設定ファイルを同時に触ると競合するだけなので、
管理役が 1 人で通す。**b と c を立てるのは工程 1 が終わってから。**

**b と c は工程 2 と工程 3 を並行して進められる。**
[問題ファイルの形式](../api/puzzle-file-format.md) が契約として先に決まっているため、
**c は生成器の完成を待たず、手書きの 1 問だけで UI を最後まで作れる**。
これが 2 並列の成立条件である。

⚠️ **難易度評価(手筋ソルバ)を 3 人目に切り出さない。**
手筋ソルバは探索ソルバと盤面表現・候補計算のビットマスクを共有するため、
分けると[同じコードを二重実装する](parallel-agent-operations.md#1-共有ロジックをまたがせない)。
切り出してよいのは **b が盤面表現と探索ソルバを固めきった後**で、
かつ手筋を大量に追加する段階に入ってからである。

⚠️ **進行の保存を c から切り離さない。** 盤面の状態そのものに触るため。

### 着手前に各 worktree で要ること

worktree は 2026-08-05 に作成済み。ただし **`node_modules` と
`.claude/settings.local.json` は worktree ごとに要る**(`ops-dev-environment-setup`)。

**工程 1 が `feature/agent-a` へ入ったら、各 worktree はまずそれを取り込む。**

```bash
git merge --ff-only feature/agent-a   # 各 worktree で
pnpm install
```

**開発サーバのポートは担当ごとに変える。** 割り当ては下表。

| 担当 | ポート |
|------|--------|
| agent-a(メイン) | 5173(既定) |
| agent-c | 5174 |

agent-b は `core` / `generator` の担当なので開発サーバを立てない。

**1 人で進めるなら worktree は要らない。** `feature/agent-a` の 1 本で、
工程 2 → 工程 3 を直列にやれば設計上どこも破綻しない。

**worktree は Git 管理外である。** ディレクトリ名の末尾のハッシュに意味は無いので、
別の端末では `git worktree add .claude/worktrees/<名前> cc/agent-b` で作り直してよい。

### 運用のルールは別文書にある

**担当の立て方・ブランチ規約・触ってよいファイルの境界・統合の作法・禁止事項は
[並列エージェントの運用](parallel-agent-operations.md) にまとめてある。**
複数のエージェントで進めるなら、体制を組む前にそれを読む。

並列で進められる単位は
[ロードマップの「並列化の単位と前提」](implementation-roadmap.md#並列化の単位と前提) にある。

### 管理役(agent-a)を引き継ぐとき

**セッションの文脈は Git に残らない。** 新しい agent-a のセッションは次の順で現在地を掴む。

1. **未統合と未コミットを測る** —— これが最優先。作業が宙に浮いていないか
   ```bash
   for b in $(git for-each-ref --format='%(refname:short)' refs/heads/cc); do
     echo "$b: $(git log --oneline feature/agent-a..$b | wc -l) 件未統合"
   done
   git worktree list | cat        # 各 worktree で git status --short も見る
   ```
2. **実測する** —— 上の「2. 現在地を実測で確かめる」。
   ロードマップの数値と合うか(合わなければ文書が古い)
3. **下の「進行中の作業」を読む** —— 各担当の次の一手が書いてある
4. 各エージェントへ再開を指示する

**管理役の仕事は 4 つ。**

| 仕事 | 中身 |
|------|------|
| 統合 | 区切りの報告が来たら即座に `feature/agent-a` へ no-ff マージ。まとまったら `develop` へ |
| 実測と数値の管理 | 統合後に測り、ロードマップと引き継ぎの数値を直す。**各自の手元の値を足しても合わない** |
| 報告の検証 | 主要な主張は**自分でも裏を取る**。鵜呑みにしない |
| 判断の集約 | 判断待ちは [保留中の判断事項](../reference/pending-decisions.md) へ。人が決めるものはユーザーへ上げる |

---

## 4. 進行中の作業

**中断すると失われる文脈**をここに書く。完了したものはロードマップへ移す。

| 誰 | 内容 | 状態 |
|----|------|------|
| agent-a | ドキュメント戦略・スキル戦略・サブエージェント戦略の導入 | ✅ 2026-08-05 に導入。cadapi-2 の構成を sudoku-web 向けに読み替えた |
| agent-a | `docs-markdown-to-html` スキルの追加と実装 | ✅ 2026-08-05。`tools/docs-html/`。決定性をテスト 11 件で固定 |
| agent-a | 工程 0(技術構成の選定と設計) | ✅ **完了(2026-08-05)**。ADR 0001〜0003 + アルゴリズム 3 本 + ファイル形式 + UI 仕様。[保留中の判断事項](../reference/pending-decisions.md) は全件決着 |
| agent-a | 工程 1(開発基盤) | ✅ **完了(2026-08-05)**。`pnpm install` → `pnpm dev` で起動を実測。テスト 12 件・型 0 エラー・Lint 0 エラー・本番ビルド成功 |
| agent-b | 工程 2 の 1〜2(盤面の表現・規則の検証・探索ソルバ) | ✅ **完了・統合済み(2026-08-05)**。所要時間を[検証](../reports/2026-08-05-search-solver-benchmark.md)へ記録 |
| agent-b | 工程 2 の 3〜4(完成盤の生成・穴あけ) | ✅ **完了・統合済み(2026-08-05)**。手がかり数の分布を[検証](../reports/2026-08-05-puzzle-generation-benchmark.md)へ記録 |
| agent-b | 工程 2 の 5(難易度評価・手筋ソルバ) | ✅ **完了・統合済み(2026-08-05)**。`core` のテスト 96 件。**レベル 1〜4 まで実装**。分布を[検証](../reports/2026-08-05-difficulty-distribution.md)へ記録 |
| agent-b | 工程 2 の 6(パック出力・並列生成) | ✅ **完了(2026-08-05)**。`core/src/puzzle-file.ts` + `generator`。**同梱パックを生成して仮置きを差し替えた**。⚠️ **agent-c の `puzzleLine.ts` / `pack.ts` は `core` へ寄せられる状態**(`decodePuzzleLine` / `decodePack` が同じ名前で入っている) |
| agent-b | 工程 2 の 7(レベル 5〜7 の手筋) | ✅ **完了(2026-08-05)**。X-Wing / Swordfish / Jellyfish / XY-Wing / XYZ-Wing / X-Chain を実装し、**5 クラス 5,000 問を収録**。評価できない問題は 41.5% → 30.1% |
| agent-c | 工程 3(`web`) | ✅ **完了・統合済み(2026-08-05)**。マニフェスト → パック → 1 問 → 入力 → 完成の判定 → 次の問題まで一本の経路が通った |
| agent-c | 工程 4(遊技機能の横展開) | ⏳ **5 項目すべて統合済み(2026-08-05)**。⚠️ **完了条件の最後の 2 点が未検証**(下記) |
| agent-c | `puzzleLine.ts` の差し替え・テーマのフラッシュ対策 | ✅ **完了・統合済み(2026-08-05)。二重実装は解消**。web のテストが 148 → 121 件へ減ったのは重複 27 件を消したため |
| agent-c | 課題 6(Mantine の部品ごと import) | ✅ **完了・統合済み(2026-08-05)。CSS が gzip 34.31 → 10.34 KB(−70%)**。⚠️ **JS は変わらない**(Mantine は部品ごとの JS の入口を配っていない) |
| agent-b | 工程 5 の一部(CI でパックの再現性を検証) | ✅ **完了・統合済み(2026-08-05)**。収録済みを作り直して `diff` するジョブを足した。**ワーカー数を収録時と変えて回すので、並列度の非依存も同時に見る** |
| agent-c | README の整備 | ✅ **完了・統合済み(2026-08-05)** |
| agent-c | コントラストの総ざらい | ✅ **完了・統合済み(2026-08-05)**。1 度差し戻した(矛盾の赤が 3 か所で 4.5:1 を割っていた)。**管理役が測り直して失格 0 を確認済み**(下記) |
| agent-c | Playwright による E2E(キーボードの 2 点) | ✅ **完了・統合済み(2026-08-05)。3 件通過・3.7 秒**。管理役が CI ジョブにし、`returnFocus` をわざと切って落ちることも確認した |
| agent-b | 工程 6 の 1(いらない機能の削除) | ✅ **完了・統合済み(2026-08-06)**。テスト 282 → 265 件。⚠️ **古い保存を読んでも遊びかけは消えない**ことを実機で確認 |
| agent-c | 工程 6 の 3〜4(あきらめる・スマートフォン対応) | ✅ **完了・統合済み(2026-08-06)**。⚠️ **1 度差し戻した**(諦めたのに「完成しました」と読み上げていた)。E2E 33 件へ |
| agent-c | `.md` の強調 128 か所の修正 | ✅ **完了・統合済み(2026-08-06)**。⚠️ **一括置換で 1 度壊して巻き戻した**(開閉が見分けられない)。**タグと空白を除いた本文の完全一致**で検証 |
| **agent-c** | **`tools/docs-lint/`(和文の強調の検査)** | 🚧 **作業中**。Node へ書き直し・誤検知を 0 にすること・壊して 1 件出ることの確認まで |
| agent-d | 工程 6 の 5(mermaid と HTML の読みやすさ) | ✅ **完了・統合済み(2026-08-06)**。⚠️ **2 度差し戻した**(ライトのコード色 4.27 / 図が縮む)。**図 4 枚・テスト 31 件** |

### ⚠️ コントラストの測り方(2026-08-05・1 度差し戻したときの記録)

**agent-c が「失格 0」と報告した状態で、管理役が測り直したら 3 件出た。**
矛盾(重複)の赤が、**強調や選択の背景と重なったとき**に 4.5:1 を割っていた。

| 組み合わせ | 差し戻し時 | **修正後(管理役の再測)** |
|---|---|---|
| 矛盾の赤 × 同じ数字の強調(ライト) | 4.19 ❌ | **4.94** ✅ |
| 矛盾の赤 × 選択中の背景(ライト) | 4.42 ❌ | **5.21** ✅ |
| 矛盾の赤 × 同じ数字の強調(ダーク) | 4.34 ❌ | **5.46** ✅ |
| 矛盾の赤 × 選択中の背景(ダーク) | 5.65 ✅ | **7.11** ✅ |

**見落としの原因は「重なった状態を作っていなかった」ことである**(agent-c の申告)。
重複した数字を**選んだまま**にしないと、矛盾のセルは強調にも選択にも入らない。

⚠️ **測り方の落とし穴が 3 つ出た。どれも「測れているつもり」になる種類である。**

| # | 落とし穴 | どうなるか |
|---|---------|-----------|
| 1 | **重なった状態を作っていない** | 地の上だけで測ると足りて見える。**4 回踏んだ** |
| 2 | **広い幅で測る** | 盤面の文字は幅で拡縮する。1280px では 28px 太字 =「大きい文字」で基準が **3:1 に緩み**、375px なら失格の色が通る。**同じ色でも幅で合否が変わる** |
| 3 | **`color-mix()` の出力を読み違える** | `color(srgb 0.71 0.15 0.15)` 形式で返るので、`rgb()` と同じに 0〜255 として扱うと**真っ黒として測る**。**agent-c と管理役が別々に踏んだ**(16.98 と 20.97 という嘘の値) |

🎯 **3 を 2 人とも自力で見つけられたのは「良すぎる値を疑った」からである。**
地の上の黒文字が 21.0 なのだから、赤がそれと並ぶはずがない。
**測定器は落ちずに嘘の値を返すので、値の妥当性を自分で問う以外に気づく方法が無い。**

⚠️ **無効の判定は `closest("[disabled],:disabled")` で祖先まで辿る。**
ボタンの中の `span.mantine-Button-label` は自分自身に `disabled` を持たないので、
要素だけを見ると**無効なボタンの中身を「有効で失格」と誤判定する**(管理役が 1 件踏んだ)。

**修正後の再測(幅 375px・両テーマ)**: ホーム / ゲーム(矛盾・誤り・強調・選択が重なった状態) /
設定モーダル のいずれも、**無効な部品を除いて失格 0 件**。

### 🎯 「人の手でしか確かめられない 2 点」は解消した(2026-08-05)

**長らく agent-c と管理役の双方が検証できずに残していた 2 点は、Playwright で埋まった。**

| # | 確かめること | 以前できなかった理由 | いま |
|---|------------|-------------------|------|
| 1 | **Enter / Space でボタンが起動するか** | ブラウザ面から送る合成キーではネイティブの活性化が起きない | ✅ E2E 2 件 |
| 2 | **モーダルを閉じたあとフォーカスが戻るか** | JS で開くと「開く前のフォーカス」が無く `returnFocus` を試せない | ✅ E2E 1 件 |

**アプリの欠陥ではなく、測る道具の制約だった。**
`pnpm test:e2e` で 3.7 秒。**CI のジョブにもしたので、壊れたら気づける。**

⚠️ **「通った」だけでは足りないので、壊して落ちることも確かめてある。**
`returnFocus={false}` にすると 3 件目が落ちた。**常に通るだけのテストではない。**

### ⚠️ 実機でしか確かめられないもの(2026-08-06 時点)

**ブラウザ面と Playwright の端末定義では届かない領域が 2 つ残っている。**

| # | 確かめること | なぜ届かないか |
|---|------------|--------------|
| 1 | **812×375(iPhone X 級の横向き)で収まるか** | **ブラウザ面は幅 768 以上にすると touch の模擬が切れる**ので `pointer: coarse` を作れない。agent-c と管理役の双方が確認した。⚠️ **条件式が当たること自体は `matchMedia` で確認済み**(`max-height: 500px` が真)。568×320 の横向きは実測で収まっている |
| 2 | **ブラウザのバーが伸縮したときの見え方**(`svh` の採用是非) | **ブラウザ面には URL バーが無く、`vh` = `dvh` = `svh` = `lvh` になる**。差を作れない。⚠️ **`svh` の採用は「同じマスを何度も見る遊びで寸法が動くのは高くつく」という理屈のままで、実測ではない** |
| 3 | **盤面の上の空きがどれくらいになるか** | 上と同じ理由。**ブラウザ面(812px)では 213px 空く**が、実機の Safari は上下のバーで 170px 前後を取るので、**41px 程度に詰まる見込み**。⚠️ **これは計算であって実測ではない** |

**⇒ 実機で触れる人がいれば、この 3 点だけ見てほしい。**

### ⚠️ プッシュするまで確かめられないもの(2026-08-06)

**`.md` の強調 128 か所を直したが、GitHub 上での見え方は誰も確認していない。**

**判断は CommonMark の flanking 規則に基づくもので、実際の表示は見ていない**
(agent-c の申告)。**手元の変換では `<strong>` 1,527 個・本文に出る `**` 0 個**まで確認済み。

⚠️ **`develop` を未プッシュのままにしているため、GitHub で開けない。**
**プッシュした時点で、いちばん `**` が多かった
[実装の進め方と現在地](implementation-roadmap.md) を開いて確かめること。**

### ⚠️ ブラウザ自動化では確かめられないもう 1 点(2026-08-05 追加)

**「システムのテーマ設定を切り替えたとき、`auto` が追随するか」は自動化では測れない。**

開発者ツールの `prefers-color-scheme` エミュレーションは、
**`matchMedia().matches` の値は変えるが `change` イベントを発火しない**
(管理役がリスナを付けて実測。エミュレーション後もイベント 0 件)。
**Mantine の `auto` は `change` の購読で追随するので、この経路では試せない。**

⚠️ **これはアプリの欠陥ではなくツールの制約である。**
再読込すれば正しいテーマになることは確認済み。
**確かめるなら OS の設定を実際に切り替えるしかない。**

**指示に含めた要点**(セッションが切れると失われるので記録する)。

| 相手 | 特に念を押したこと |
|------|------------------|
| agent-b | 乱数はシード注入 / 同型変換で量産しない / 難易度を手がかり数で決めない / 手筋ソルバは決定的に / **TypeScript での生成性能を実測して報告** |
| agent-c | 盤面は自前実装 / 状態遷移は reducer / キーボードだけで遊べる / **`core` に足りない関数は自分で足さず管理役へ** / **数独のロジックを `web` に書かない** |

**c が b を待たなくてよい根拠も伝えてある** —— 契約の 1 行に解が含まれているので、
完成判定にソルバが要らない。

### 問題パックの置き場所と作り直しかた

**仮置き(同型変換で作った 3 問)は 2026-08-05 に生成器の出力へ差し替え済みである。**
いまの `puzzles/` は `@sudoku/generator` が作った本物で、
やさしい・ふつう・むずかしい が各 1,000 問(1 パック約 172 KB)。
**難問・最難関は 0 問**(レベル 5 以降の手筋が未実装のため)。

```bash
pnpm --filter @sudoku/generator generate --difficulty easy,normal,hard --count 1000
```

**同じ指定からは 1 バイト違わず同じファイルができる**(`--workers` を変えても同じ)。
⚠️ **手筋を実装したらパックを作り直す。** 難易度の基準が変わるためである。

**配線は決着した(2026-08-05・管理役)。** 正本は設計どおり**リポジトリ直下の `puzzles/`** で、
`tools/puzzles-sync/sync.mjs` が `packages/web/public/puzzles/` へ写す
(`pnpm dev` と `pnpm build` の先頭で走る)。

| 場所 | 扱い |
|------|------|
| `puzzles/manifest.json` と `puzzles/packs/` | **正本**。Git 管理下 |
| `puzzles/generated/` | 大量生成した追加分。**Git 管理外。配信物にも含めない** |
| `packages/web/public/puzzles/` | **複製**。Git 管理外。**手で編集しない**(次の同期で消える) |

**⇒ agent-b は `puzzles/` へ書き出せばよい**。`web` 側の配線は済んでいる。

### agent-c が上げた設計との食い違い 2 件 —— ✅ 決着(2026-08-05・管理役)

**c は[境界の規約](parallel-agent-operations.md#触ってよいファイルの境界)に従い、
実装を進めつつ文書を直さずに上げてきた。** 判断は次のとおり。

| # | 食い違い | 決定 |
|---|---------|------|
| 1 | [システム構成](../architecture/system-architecture.md) は盤面の reducer を `core` に置くと読めるが、境界では `core` は b が持つ | **`packages/web/src/state/` に置いたままとし、文書のほうを直した**。`core` は**生成側と遊技側が共有するもの**だけを置く場所で、遊技中の状態(選択・メモ・取り消し)は生成器が一切使わない。**React に依存させない方針は維持する** |
| 2 | 同文書は問題ファイルの読み書きを `core` の責務としているが `core` にまだ無い | **文書どおり `core` が持つ**。形式は**生成側と遊技側の両方が読む契約**なので、こちらは共有が正しい。**agent-b への依頼として伝達済み**。入ったら web 側の暫定実装(`features/puzzle/puzzleLine.ts`)を消して差し替える |

**分かれ目は「生成器も使うか」。** 使うなら `core`、遊技だけなら `web`。

⚠️ **2 の受け渡しが進行中(2026-08-05)。** `core` に**盤面と 81 文字の相互変換**が入った
(`board.ts`)。ただし**パックの 1 行(`手がかり,解,難易度,スコア`)を読む部分はまだ**で、
web の暫定実装が生きている。**b がパック出力を実装するときに揃える。**
### 設計時に確かめた事実で、失うと痛いもの

**セッションの文脈は Git に残らない。** 次の 4 点は調査で裏が取れており、
**知らずに実装すると作り直しになる**。詳細は
[調査: UI ライブラリ](../reports/2026-08-05-ui-library-survey.md) と
[調査: 数独の生成](../reports/2026-08-05-sudoku-generation-survey.md)。

- **同型変換で完成盤を量産してはいけない**(難易度も解き筋も同一になる)
- **手がかり数は難易度の指標にならない**(人間の体感との相関 0.25〜0.27)
- **Mantine 9 は React 19.2+ が必須**(React 18 へ戻れない)
- **手筋を足しても、既に評価できていた問題の難易度は変わらない。**
  簡単な手筋から順に試すので上位の手筋に出番が来ないためで、
  レベル 5〜7 を足した後もやさしい・ふつう・むずかしいの問数は 1 問も動かなかった
  ([検証](../reports/2026-08-05-difficulty-distribution.md))。
  ⚠️ **ただし「評価できない」だった問題は上のクラスへ移るので、
  手筋を足したらパックを作り直す**
- **生成性能の公開値はすべて C++ / Java のもの**。
  同じ出典に「JavaScript 版は非常に遅い」とあったが、
  **探索ソルバは TypeScript でも実用速度が出た**(一意解の判定は 1 回あたり数十〜数百 μs。
  [検証](../reports/2026-08-05-search-solver-benchmark.md))。
  **生成 1 問あたりの時間はまだ測っていない**
